global:
  resolve_timeout: 2m

route:
  receiver: 'webhook_receiver'
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: 'webhook_receiver'
    webhook_configs:
      - url: 'http://192.168.56.1:5000/alerts'
        send_resolved: true