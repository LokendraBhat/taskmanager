global:
  resolve_timeout: 5m

route:
  receiver: 'receivers_default'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'webhook_receiver'
    webhook_configs:
      - url: 'http://your-webhook-endpoint.local/alerts'
        send_resolved: true