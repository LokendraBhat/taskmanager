global:
  resolve_timeout: 5m

route:
  receiver: 'webhook_receiver'
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h

receivers:
  - name: 'webhook_receiver'
    webhook_configs:
      - url: 'http://192.168.56.1:5000/alerts'
        send_resolved: true